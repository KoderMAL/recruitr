<%= form_with(model: available_position, local: true) do |form| %>
  <% if available_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(available_position.errors.count, "error") %> prohibited this available_position from being saved:</h2>

      <ul>
        <% available_position.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <%= form.fields_for :skills do |s| %>
    <%= s.label :skill %>
    <%= s.text_field :content %>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
